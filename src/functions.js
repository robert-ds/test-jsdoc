/**
 * This function sum two numbers and return result
 * @param {Number} n1 first number
 * @param {Number} n2 second number
 * @returns {Number} n1 + n2
 */
function add(n1, n2) {
  return n1 + n2;
}

/**
 * Write personality message
 * @param {String} msg message
 * @returns {String} You Message
 */
const perMsg = msg => { return "message: " + msg };
console.log(perMsg("Un caluroso abrazo"));